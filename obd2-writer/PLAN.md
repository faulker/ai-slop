# OBD2 read/write app

A app that supports the ability to read from and write to a vehicles computer via the obd2 port. The hardware being used is a OBDLink MX+ OBD2 Bluetooth Scanner.

The app should have the ability to read and write to a 2023 Toyota Tacoma.

You will need to search the web to find the specs for the Toyota
