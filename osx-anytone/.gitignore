/target
Cargo.lock
# Codeplug backups may contain personal data (callsign, contacts).
*.bin
# Captured radio dumps and vendor CPS protocol traces. Same personal data, and
# the trace files have no extension so *.bin does not cover them.
/docs/reference/
# Xcode/app build artifacts (project is regenerated by xcodegen).
/build
/AnyToneMac.xcodeproj
/libanytone_core.a
build_output.log
